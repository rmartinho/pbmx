{
  "scripts": {
    "start": "snowpack dev",
    "build-wasm": "(cd .. && wasm-pack build --release --target web --no-typescript) && npm install ../pkg",
    "build": "snowpack build",
    "test": "echo \"Vue 3.0 test support is still in progress.\" && exit 1"
  },
  "dependencies": {
    "pbmx-web": "file:../pkg",
    "vue": "^3.0.0-0"
  },
  "devDependencies": {
    "@snowpack/app-scripts-vue": "^1.1.4",
    "snowpack": "^2.2.0"
  },
  "snowpack": {
    "extends": "@snowpack/app-scripts-vue",
    "exclude": [
      "*.sw?"
    ],
    "devOptions": {
      "bundle": false
    }
  }
}
